export interface Recipe {
    label: string;
    url: string;
    calories?: number;
    image?: string;   
}

interface Practice {
    label: string;
    url: string;
    calories?: number;
    image?: string;
}